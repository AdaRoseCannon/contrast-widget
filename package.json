{
  "name": "contrast-widget",
  "version": "1.0.0",
  "description": "Bookmarklet to analyse the contrast of test elements in a document",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p build && rollup -c && babel --presets es2015 ./build/bundle.es2015.js | uglifyjs -m --screw-ie8 > ./build/bundle.min.js && wc -c ./build/bundle.min.js",
    "watch": "nodemon --watch ./lib --exec 'npm run build'"
  },
  "author": "Ada Rose Edwards <ada@ada.is>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0",
    "chart.js": "^1.0.2",
    "nodemon": "^1.8.1",
    "preprocess": "^3.0.2",
    "rollup": "^0.25.3",
    "rollup-plugin-commonjs": "^2.2.1",
    "rollup-plugin-node-resolve": "^1.4.0",
    "uglify-js": "^2.6.1"
  }
}
